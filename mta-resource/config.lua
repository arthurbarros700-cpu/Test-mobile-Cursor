-- Copie este resource para o servidor MTA e ajuste os valores.
-- O bot Node.js deve estar acessível a partir do host do MTA (IP público ou rede interna).

config = {
    bridgeURL = "http://127.0.0.1:3750",
    bridgeSecret = "troque-esta-chave-secreta-longa",
}
